export interface Translations {
  meta: {
    title: string
    description: string
  }
  nav: {
    about: string
    timeline: string
    skills: string
    projects: string
    contact: string
  }
}
